-- Paranoidal: Bob's spawners disabled — only Rampant-managed spawners are used.
-- Vanilla biter-spawner/spitter-spawner are kept so RampantFixed can upgrade them.
function fillBobEnemies(config)
end